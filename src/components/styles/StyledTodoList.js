import styled from "styled-components";

export const StyledTodoList = styled.section`
    position: relative;

    > label {
        position: absolute;
        top: -50px;
        left: 18px;
    }
`
